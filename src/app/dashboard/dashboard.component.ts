import { Component, NgZone, ElementRef, ViewChild, OnInit, PLATFORM_ID, Inject, Injector} from '@angular/core';


@Component({
  selector: "dashboard",
  styleUrls: ["dashboard.component.css"],
  templateUrl: "dashboard.component.html"
})

export class DashboardComponent implements OnInit {
  ngOnInit(): void {
    
  }

}
